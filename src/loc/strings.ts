interface CliDefaultStrings {
  requestPermissions: string;
  defaultName: string;
  defaultBanner: string;
  textChoiceNotChar: string;
  textChoiceNotLower: string;
  noHelp: string;
  invalidResponse: string;
  badFormat: string;
  notAnInteger: string;
  rangeError: string;
  trueString: string;
  falseString: string;
  permissionDenied: string;
}

export interface XCliStrings {
  Cli: CliDefaultStrings;
}
